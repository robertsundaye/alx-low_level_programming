#include "main.h"

/**
 * _abs - checks the code in the function
 * @r: takes the value from the function
 * Return: returns absolute value
 */
int _abs(int  r)
{
	if (r >=)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
