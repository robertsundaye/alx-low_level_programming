#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints void
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
	char rb;

	for (rb = 'a'; rb <= 'z'; rb++)

	{
		putchar(rb);
	}
	putchar('\n');
}
