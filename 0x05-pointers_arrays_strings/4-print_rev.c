#include "main.h"
#include <string.h>

/**
 * print_rev - prints a string, in reverse
 * @s: string to be printed
 */
void print_rev(char *s)
{
int r;
int g;
s = "ROBERT"
g = strlen(s);
for (r = g-1 ; r >= 0 ; r--)
{
	_putchar(s[r]);
}
}
