#include "main.h"

/**
 * mul - checks the code in the function
 * @a: takes value as input
 * @b: takes value as input
 * Return: returns c as @a and @b product
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
